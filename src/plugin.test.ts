import { backstagePluginConvisoPlugin } from './plugin';

describe('backstage-plugin-conviso', () => {
  it('should export plugin', () => {
    expect(backstagePluginConvisoPlugin).toBeDefined();
  });
});
